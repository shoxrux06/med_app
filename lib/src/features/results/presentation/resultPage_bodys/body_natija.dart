import 'package:flutter/material.dart';

class NatijaBodys extends StatelessWidget {
  const NatijaBodys({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('natija'),
    );
  }
}
